---
name: unlimited-subagent-turns
description: Allow subagents unlimited turns.
disable-model-invocation: true
---

# Unlimited Subagent Turns

Spawn and resume every subagent without a turn cap. Omit turn-limit arguments such as `max_turns` when omission means unlimited; otherwise use the harness's documented unlimited setting. Report any hard cap the harness cannot disable.
