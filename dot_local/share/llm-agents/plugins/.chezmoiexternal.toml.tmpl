["superpowers"]
    type = "archive"
    url = "{{ .external_resources.pins.superpowers.url }}"
    stripComponents = 1
    exact = true
    checksum.sha256 = "{{ .external_resources.pins.superpowers.sha256 }}"

["mattpocock-skills"]
    type = "archive"
    url = "{{ .external_resources.pins.mattpocock_skills.url }}"
    stripComponents = 1
    exact = true
    checksum.sha256 = "{{ .external_resources.pins.mattpocock_skills.sha256 }}"
